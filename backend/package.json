{
    "name": "lida-backend",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "wrangler dev --env dev",
        "deploy": "wrangler deploy",
        "types": "wrangler types",
        "db:migrate": "wrangler d1 migrations apply CONTENT_DB --local --env dev"
    },
    "devDependencies": {
        "wrangler": "^4.131.1"
    }
}
