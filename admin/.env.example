# Production uses the deployed Worker URL when this is omitted. Override it for
# another environment with the Worker origin, without the trailing /api.
VITE_API_BASE_URL=https://lida-backend.onochieazukaeme.workers.dev
VITE_SITE_URL=http://localhost:3000
